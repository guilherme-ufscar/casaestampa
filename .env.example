# Neon PostgreSQL — copie a "Connection string" do painel do Neon (modo pooled)
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/casaestampa?sslmode=require"

# Neon PostgreSQL — copie a "Direct connection string" do painel do Neon (sem pooler)
# Necessária para rodar migrations
DIRECT_URL="postgresql://user:password@ep-xxx-direct.neon.tech/casaestampa?sslmode=require"

NEXTAUTH_SECRET="troque-por-uma-string-aleatoria-longa"
NEXTAUTH_URL="https://seudominio.com.br"
